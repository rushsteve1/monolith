# SOCon: Steve's Overseer Controller

A CLI tool that communicates with the [`overseer`](../overseer/) to control and
administrate it. It is effectively an admin interface but without all the
difficulties of authentication or security concerns of a web panel.
