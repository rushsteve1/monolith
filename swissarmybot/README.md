# SwissArmyBot

I don't know how many versions of this bot there have been. I think this is
the 10<sup>th</sup> or so. This bot also has a web interface which is heavily
used to simplify the bot itself.

To run this standalone compile it using the [`main` submodule](./main/).
